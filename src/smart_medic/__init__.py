"""Smart Medic deterministic medical concept extraction pipeline."""

# ``__version__`` remains the frozen v3 path's manifest version.  The v4 path
# is opt-in and records its own version without changing v3 reproducibility.
__version__ = "3.3.0"
V4_VERSION = "4.0.0"
