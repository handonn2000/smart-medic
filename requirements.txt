# Pinned to the versions verified working for this project.
# For NVIDIA GPU (CUDA), install the matching torch build first, e.g.:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# then run: pip install -r requirements.txt

torch==2.13.0
torchvision==0.28.0
torchaudio==2.11.0
transformers==5.14.1
datasets==5.0.0
seqeval==1.2.2
underthesea==9.5.0
pytorch-crf==0.7.2
accelerate==1.14.0
rapidfuzz==3.14.5
pandas==3.0.5
requests==2.34.2
