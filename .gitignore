# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Model artifacts (large binaries — track via git-lfs or release assets if needed)
models/*.bin
models/*.pt
models/*.safetensors
models/*.onnx
!models/.gitkeep

# Generated predictions
data/output/*.json
!data/output/.gitkeep

# Bài nộp — dẫn xuất, luôn sinh lại được bằng `smk solve --zip`
data/submission/

# KB pipeline — dữ liệu dẫn xuất, luôn build lại được từ nguồn thô
data/staging/*
!data/staging/.gitkeep
data/artifacts/*
!data/artifacts/.gitkeep

# Cache model của HuggingFace (Phase 5) — tải một lần, không vào git
.cache/
models/hf/

# Test / lint cache
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment / secrets
.env
.env.*

# OS / editor
.DS_Store
.idea/
.vscode/


data/knowledge_base/icd
data/knowledge_base/rxnorm
data/knowledge_base/snomed
