# Add project dependencies here, e.g.:
# torch
# transformers
# pandas
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install transformers datasets seqeval underthesea torchcrf accelerate
pip install rapidfuzz pandas requests