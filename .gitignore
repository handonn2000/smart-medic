# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Model artifacts (large binaries — track via git-lfs or release assets if needed)
models/*.bin
models/*.pt
models/*.safetensors
models/*.onnx
!models/.gitkeep

# Generated predictions
data/output/*.json
!data/output/.gitkeep

# Environment / secrets
.env
.env.*

# OS / editor
.DS_Store
.idea/
.vscode/

# KB đã build — sinh lại bằng: python3 src/smart_medic/kb/build.py
# (bỏ comment 3 dòng dưới nếu muốn commit artifact cho BTC chạy ngay)
data/kb/*.csv.gz
!data/kb/MANIFEST.json
data/output.zip
